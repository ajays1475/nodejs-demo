//chalk is a package which help to add colours in text
//var chalk = require("chalk");
import chalk from 'chalk';
console.log(chalk.yellow('Starting server in dev mode...')); // eslint-disable-line no-console
